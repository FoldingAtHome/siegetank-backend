#! /usr/bin/env python

import os
import signal

pids = open('pids.log').read().split(' ')

for pid in pids:
    try:
        os.killpg(int(pid), signal.SIGTERM)
    except Exception:
        pass

os.system('rm -rf pids.log target_ids.log aof_* streams targets')
