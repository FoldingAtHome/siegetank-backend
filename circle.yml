checkout:
  post:
    - cd server
    - wget https://raw2.github.com/proteneer/apollo/master/apollo.py

dependencies:
  pre:
    - pip install tornado
    - pip install requests
    - pip install redis